<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.neusoft.coffeeshop"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <uses-permission
        android:name="android.permission.CALL_PHONE"
        android:maxSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.StartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.Welcome" >
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/home" />
        <activity
            android:name=".viewflow.DetailActivity"
            android:label="@string/detail" />
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.ShopActivity"
            android:label="@string/shop" />
        <activity
            android:name=".activity.ShopDetailActivity"
            android:label="@string/shopDetail" />
        <activity
            android:name=".activity.UpdateShopActivity"
            android:label="@string/shopUpdate" />
        <activity
            android:name=".activity.AddShopActivity"
            android:label="@string/shopAdd" />
    </application>

</manifest>